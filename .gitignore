# Dependências
/node_modules

# Artefatos de build do Docusaurus
/build
/.docusaurus
/.cache-loader

# Saída da verificação do motor de benefícios (npm run verificar)
/.verificar-build

# Configuração local de ferramentas
/.claude

# Conferidor: páginas baixadas do Planalto e relatórios de cada rodada.
# São reproduzíveis (scripts/crawler/baixar.py) e pesam ~5 MB por rodada; na
# CI viram artifact. As DECISOES-*.md do diretório continuam versionadas.
/crawler/snapshots
/crawler/relatorios

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
__pycache__/
